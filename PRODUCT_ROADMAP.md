# CareerPilot-CN 产品化路线图

## 产品目标

把当前以命令和提示词驱动的求职工作流，收敛成一个可重复、可解释、可验证、保护隐私的个人求职操作系统。核心闭环是：建立候选人事实档案 → 发现并筛选岗位 → 生成可验证的申请材料 → 跟踪结果 → 用结果改进下一次匹配。

## 当前判断

- **已有基础：** 中文候选人档案、岗位抓取与去重、契合度评估、简历/求职信定制、ATS 检查、申请归档、结果回写、邮件同步和 HTML/Notion 报表。
- **主要缺口：** 状态文件和命令之间仍依赖人工维护；缺少统一的质量评分和错误恢复；尚未形成真正的可视化产品界面；个人数据的脱敏、备份和导出流程还可以更明确。`/doctor` 和申请 manifest 已覆盖第一批诊断与追溯需求。
- **产品原则：** 事实优先、人工确认关键写入、每个结论可追溯、失败可恢复、默认本地存储、自动化不替用户投递。

## 分阶段计划

### P0：可靠性基线（本次实施）

- 增加 `/doctor` 环境与数据体检，提前发现缺失档案、模板、编译器和状态文件问题。
- 明确每次简历生成的 ATS 预检、事实来源和待确认项。
- 在 README 中提供成熟产品的使用闭环和故障排查入口。

### P1：求职工作台（下一阶段）

- 统一 `job_search_tracker.csv`、归档目录和 `seen_jobs.json` 的状态模型，提供 schema 版本和迁移检查。（申请 manifest 已先行提供 schema_version。）
- 增加岗位看板：待评估、已匹配、已起草、已申请、面试、Offer、已结束。
- `/board` 已先提供行动队列规范，并新增 `tools/generate_board.py` 作为离线生成器；后续再升级为可交互页面。
- 为每次申请生成可追溯的 run manifest：岗位来源、档案版本、简历版本、求职信版本、检查结果和用户确认记录。

### P2：材料质量与个性化

- 把 ATS 关键词覆盖、事实一致性、页面质量、岗位要求覆盖统一为可解释评分，不用单一黑盒分数替代明细。
- 建立主张—证据账本的可视化审阅页，支持冲突、过期和待确认状态。
- 根据投递结果和面试反馈，生成“哪些岗位/技能/表述更有效”的复盘报告。

### P3：交互产品化

- 提供本地 Web 工作台，覆盖档案编辑、岗位导入、申请审阅、材料预览和结果记录。
- 所有写入操作保留撤销、备份和导出；默认离线运行，外部服务调用逐项提示。
- 增加可访问性、中文字体、移动端预览和打印回归测试。

## 验收指标

- 新用户能在一次体检后知道缺什么，以及下一步怎么补齐。
- 每份最终简历都能回答：事实来自哪里、岗位关键词覆盖哪些、还有哪些诚实缺口。
- 任何自动化写入都可定位到命令、时间和原始材料，不覆盖用户手工内容。
- 生成失败时能保留草稿并给出可执行的修复建议，而不是静默失败。
